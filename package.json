{
  "name": "agroflourish-repo",
  "version": "0.0.0",
  "private": true,
  "description": "AgroFlourish monorepo – install and run from app directories (AgroFlourish or Tenant-Nexus)",
  "scripts": {
    "install:all": "cd AgroFlourish && pnpm install && cd ../Tenant-Nexus && pnpm install"
  }
}
